import 'package:flutter/material.dart';

class LabTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Lab Test Page")));
  }
}
